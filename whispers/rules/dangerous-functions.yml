- id: dangerous-functions
  description: Dangerous function calls that could lead to RCE
  message: Dangerous Function
  severity: MINOR
  key:
    regex: ^function$
    ignorecase: False
  value:
    regex: ^(eval|exec|(os.)?system|popen|call|subprocess\.(check_output|call))\(
    ignorecase: True